{
  "buildCommand": "cd src/web/command-center && npm install && npm run build",
  "outputDirectory": "src/web/command-center/dist",
  "installCommand": "echo skip",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
