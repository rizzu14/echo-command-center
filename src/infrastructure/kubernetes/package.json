{
  "name": "@echo/kubernetes",
  "version": "1.0.0",
  "description": "ECHO Kubernetes tenant provisioning framework",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "test": "vitest --run"
  },
  "dependencies": {
    "@kubernetes/client-node": "^0.21.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "vitest": "^1.0.0",
    "@types/node": "^20.0.0"
  }
}
