{
  "name": "@echo/cassandra-infrastructure",
  "version": "0.1.0",
  "private": true,
  "description": "Cassandra Liquid Ledger storage for ECHO — hash-chained immutable audit log",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run"
  },
  "dependencies": {
    "cassandra-driver": "^4.7.2"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.7.3",
    "vitest": "^3.2.4"
  }
}
